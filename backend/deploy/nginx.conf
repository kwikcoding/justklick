server {
    listen 80;
    server_name api.justklick.co.in;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$host$request_uri;

    location / {
        include proxy_params;
        proxy_pass http://unix:/var/www/api.justklick.co.in/backend/gunicorn.sock;
    }

    # Serve static files
    location /static/ {
        alias /var/www/api.justklick.co.in/backend/staticfiles/;
    }

    # Serve media files (if any)
    location /media/ {
        alias /var/www/api.justklick.co.in/backend/media/;
    }
}

# HTTPS configuration (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     server_name api.justklick.co.in;
#
#     ssl_certificate /etc/letsencrypt/live/api.justklick.co.in/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.justklick.co.in/privkey.pem;
#
#     # SSL settings
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     location / {
#         include proxy_params;
#         proxy_pass http://unix:/var/www/api.justklick.co.in/backend/gunicorn.sock;
#     }
#
#     # Serve static files
#     location /static/ {
#         alias /var/www/api.justklick.co.in/backend/staticfiles/;
#     }
#
#     # Serve media files (if any)
#     location /media/ {
#         alias /var/www/api.justklick.co.in/backend/media/;
#     }
# }
